{
  "name": "@udp/server-utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "pnpm run build && node test-runner.js"
  },
  "dependencies": {
    "@udp/db": "workspace:*",
    "next-auth": "^4.24.12",
    "@auth/core": "^0.34.3"
  },
  "devDependencies": {
    "@prisma/client": "^7.2.0",
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  }
}
