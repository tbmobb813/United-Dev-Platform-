describe('packages/config - sanity', () => {
  it('basic arithmetic sanity check', () => {
    expect(1 + 1).toBe(2);
  /* eslint-env jest */

  describe('packages/config - sanity', () => {
    it('basic arithmetic sanity check', () => {
      expect(1 + 1).toBe(2);
    });
  });
