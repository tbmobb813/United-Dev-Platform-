name: auto-fix-disabled

# Intentionally disabled for local `act` runs. Restore the full workflow
# in the remote repository to enable GitHub Actions.

on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: no-op
        run: echo "auto-fix workflow disabled for local testing"
